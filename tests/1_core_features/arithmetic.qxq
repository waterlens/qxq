(* RUN: {qxq|cargo run --} %s *)
1 + 2 * 3 - 4 / 5;
(1 + 2) * 3;
+1 - 2;
(1, 2, 3);
1 < 2;
1 > 2;
1 <= 2;
1 >= 2;
1 == 2;
1 != 2

(* EXPECT:
   --- Syntax Tree ---
   (block (- (+ 1 (* 2 3)) (/ 4 5)) (* (+ 1 2) 3) (- 1 2) (tuple 1 2 3) (< 1 2) (> 1 2) (<= 1 2) (>= 1 2) (== 1 2) (!= 1 2))
   --- Constant Pool ---
   --- Integer Constants ---
   @0: 2
   @1: 3
   @2: 1
   @3: 4
   @4: 5
   --- String Constants ---
   
   --- Thunk ---
   thunk::__top_thunk__ captured::[]
   loadc        r0, @0
   loadc        r1, @1
   mul.dd       r0, r0, r1
   loadc        r1, @2
   add.dd       r0, r1, r0
   loadc        r1, @3
   loadc        r2, @4
   div.dd       r1, r1, r2
   sub.dd       r0, r0, r1
   loadc        r0, @2
   loadc        r1, @0
   add.dd       r0, r0, r1
   loadc        r1, @1
   mul.dd       r0, r0, r1
   loadc        r0, @2
   loadc        r1, @0
   sub.dd       r0, r0, r1
   loadc        r0, @2
   loadc        r1, @0
   loadc        r2, @1
   wrap         r0, k1, #3
   loadc        r0, @2
   loadc        r1, @0
   setc         r0
   cmp.lt.dd    r0, r1
   loadc        r0, @2
   loadc        r1, @0
   setc         r0
   cmp.gt.dd    r0, r1
   loadc        r0, @2
   loadc        r1, @0
   setc         r0
   cmp.le.dd    r0, r1
   loadc        r0, @2
   loadc        r1, @0
   setc         r0
   cmp.ge.dd    r0, r1
   loadc        r0, @2
   loadc        r1, @0
   setc         r0
   cmp.eq.dd    r0, r1
   loadc        r0, @2
   loadc        r1, @0
   setc         r0
   cmp.ne.dd    r0, r1
   retn         r0, #0
*)
