(* RUN: printf 'abcd' > /tmp/qxq_file_edit_existing.bin && {qxq|cargo run --} %s && xxd -p /tmp/qxq_file_edit_existing.bin && rm -f /tmp/qxq_file_edit_existing.bin *)

let f = File.open("/tmp/qxq_file_edit_existing.bin");
let start = f.size();
f.close();

if start == 4 then 0 else start end

(* EXPECT:
   0
   61626364
*)
