(* RUN: {qxq|cargo run --} %s *)
1 + 2 * 3 - 4 / 5;
(1 + 2) * 3;
+1 - 2;
(1, 2, 3);
1 < 2;
1 > 2;
1 <= 2;
1 >= 2;
1 == 2;
1 != 2

(* EXPECT:
   --- Syntax Tree ---
   (block (- (+ 1 (* 2 3)) (/ 4 5)) (* (+ 1 2) 3) (- 1 2) (tuple 1 2 3) (< 1 2) (> 1 2) (<= 1 2) (>= 1 2) (== 1 2) (!= 1 2))
   --- Constant Pool ---
   --- Integer Constants ---
   @0: 2
   @1: 3
   @2: 1
   @3: 4
   @4: 5
   --- String Constants ---
   
   --- Thunk ---
   thunk::__top_thunk__ captured::[]
   loadc        r0, @0
   loadc        r1, @1
   mul.dd       r0, r0, r1
   loadc        r1, @2
   add.dd       r0, r1, r0
   loadc        r1, @3
   loadc        r2, @4
   div.dd       r1, r1, r2
   sub.dd       r0, r0, r1
   loadc        r1, @2
   loadc        r2, @0
   add.dd       r1, r1, r2
   loadc        r2, @1
   mul.dd       r1, r1, r2
   loadc        r2, @2
   loadc        r3, @0
   sub.dd       r2, r2, r3
   loadc        r3, @2
   loadc        r4, @0
   loadc        r5, @1
   wrap         r3, k1, #3
   loadc        r4, @2
   loadc        r5, @0
   loadc        r4, @2
   loadc        r5, @0
   loadc        r4, @2
   loadc        r5, @0
   loadc        r4, @2
   loadc        r5, @0
   loadc        r4, @2
   loadc        r5, @0
   loadc        r4, @2
   loadc        r5, @0
   setc         r4
   cmp.ne.dd    r4, r5
   retn         r4, #0
*)
