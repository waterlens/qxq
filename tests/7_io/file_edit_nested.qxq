(* RUN: rm -f /tmp/qxq_file_edit_nested.bin && {qxq|cargo run --} %s && rm -f /tmp/qxq_file_edit_nested.bin *)

let size = fn (path)
  let f = File.open(path);
  let start = f.size();
  f.close();
  start
end;

size("/tmp/qxq_file_edit_nested.bin")

(* EXPECT:
   0
*)
