(* RUN: {qxq|cargo run --} %s *)
if 1 < 2 then 3 else 4 end;
9 / 10
(* EXPECT:
   --- Syntax Tree ---
   (block (if (< 1 2) 3 4) (/ 9 10))
   --- Thunk ---
   thunk::__top_thunk__ params::0 regs::2 captured::[]
   constants::[
     @0: 1
     @1: 2
     @2: 9
     @3: 10
   ]
   loadc        r0, @0
   loadc        r1, @1
   cmp.lt.dd    r0, r1
   jmp          #1
   jmp          #0
   loadc        r0, @2
   loadc        r1, @3
   div.dd       r0, r0, r1
   retn         r0, #0
*)
